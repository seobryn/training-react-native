import { StyleSheet } from 'react-native'

const LinkStyles = StyleSheet.create({
    text: {
        color: '#FA4A0C',
        fontWeight: '700',
        letterSpacing: 1.5
    },
})

export default LinkStyles
